provider_installation {
  filesystem_mirror {
    path    = "/home/accassar/.terraform.d/plugins"
    include = ["registry.terraform.io/peakhour-io/*"]
  }
  direct {
    exclude = ["registry.terraform.io/peakhour-io/*"]
  }
}
