---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "peakhour_rp_waf_custom_rule_order Resource - peakhour"
subcategory: ""
description: |-
  Manages the order of WAF custom rules for a domain. By default it expects the complete order (include_all_rules=true), which surfaces out-of-band additions and removals as drift.
---

# peakhour_rp_waf_custom_rule_order (Resource)

Manages the order of WAF custom rules for a domain. By default it expects the complete order (include_all_rules=true), which surfaces out-of-band additions and removals as drift.

## Example Usage

```terraform
resource "peakhour_rp_waf_custom_rule_order" "example" {
  domain            = "example.com"
  include_all_rules = false
  order             = ["11111111-1111-1111-1111-111111111111"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) Domain name.
- `order` (List of String) WAF custom rule UUIDs in desired order. When include_all_rules=true, this must be the complete order. When false, it is treated as a managed subset.

### Optional

- `include_all_rules` (Boolean) If true, order must include all WAF custom rules currently configured for the domain. If false, only the relative order of the listed rules is managed, allowing rule additions and removals in the same apply.

### Read-Only

- `id` (String) WAF custom rule order identifier (domain/customrule_order).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import peakhour_rp_waf_custom_rule_order.example example.com
```
