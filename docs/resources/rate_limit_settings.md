---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "peakhour_rate_limit_settings Resource - peakhour"
subcategory: ""
description: |-
  Manages rate limit mode settings for a domain (e.g., global, vhost, zone). This controls which rate limiting systems are active.
---

# peakhour_rate_limit_settings (Resource)

Manages rate limit mode settings for a domain (e.g., global, vhost, zone). This controls which rate limiting systems are active.

## Example Usage

```terraform
resource "peakhour_rate_limit_settings" "example" {
  domain = "example.com"
  mode   = ["zone", "global"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) Domain name.

### Optional

- `mode` (List of String) Enabled rate limit modes (spec enum: none, global, vhost, vhost-busy, zone, client-errors, server-errors, all).

### Read-Only

- `id` (String) Rate limit settings identifier (domain/rate_limit_settings).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import peakhour_rate_limit_settings.example example.com
```
