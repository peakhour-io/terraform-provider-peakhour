---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "peakhour_rp_waf_custom_rule Resource - peakhour"
subcategory: ""
description: |-
  Manages a reverse proxy WAF custom rule for a domain. Complex fields are represented as JSON.
---

# peakhour_rp_waf_custom_rule (Resource)

Manages a reverse proxy WAF custom rule for a domain. Complex fields are represented as JSON.

## Example Usage

```terraform
resource "peakhour_rp_waf_custom_rule" "example" {
  domain      = "example.com"
  description = "Log curl user agents"
  enabled     = true

  rules_json = jsonencode([{
    variable      = "REQUEST_HEADERS"
    variable_part = "user-agent"
    operator      = "@contains"
    operator_arg  = "curl"
  }])

  action_json = jsonencode({
    action_name = "pass"
  })

  logging_json = jsonencode({
    message  = "curl user agent"
    severity = "INFO"
    tags     = ["terraform"]
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action_json` (String) Action as JSON object (WafAction). Server-side defaults are ignored for drift detection.
- `domain` (String) Domain name.
- `logging_json` (String) Logging as JSON object (WafLogging). Server-side defaults and null-valued object properties are ignored for drift detection; null array elements remain significant.
- `rules_json` (String) Rules as JSON array (WafCustomRuleExpression[]). Server-side defaults and null-valued object properties are ignored for drift detection; null array elements remain significant.

### Optional

- `description` (String) Custom rule description. Set to null to clear.
- `enabled` (Boolean) Whether the rule is enabled.

### Read-Only

- `created` (String) Creation timestamp (RFC3339, computed).
- `id` (String) Custom rule identifier (domain/customrule/uuid).
- `rule_id` (Number) Numeric rule ID (computed).
- `uuid` (String) Custom rule UUID (computed after creation).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import peakhour_rp_waf_custom_rule.example example.com/customrule/11111111-1111-1111-1111-111111111111
```
