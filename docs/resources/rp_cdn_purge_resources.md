---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "peakhour_rp_cdn_purge_resources Resource - peakhour"
subcategory: ""
description: |-
  Triggers a reverse proxy CDN purge for specific resources (action). This is not stable desired-state; changing inputs will re-run the purge.
---

# peakhour_rp_cdn_purge_resources (Resource)

Triggers a reverse proxy CDN purge for specific resources (action). This is not stable desired-state; changing inputs will re-run the purge.

## Example Usage

```terraform
resource "peakhour_rp_cdn_purge_resources" "example" {
  domain = "example.com"
  run_id = "deploy-2026-08-26"
  paths  = ["/assets/app.css", "/assets/app.js"]
  soft   = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) Domain name.
- `run_id` (String) An arbitrary identifier to force a new purge (e.g. timestamp or ticket number).

### Optional

- `paths` (List of String) Paths to purge. Set to null to purge all resources.
- `soft` (Boolean) Whether to use soft purge semantics. Set to null to use API default.

### Read-Only

- `id` (String) Purge identifier (domain/rp_cdn_purge_resources/run_id).
