---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "peakhour_acme_settings Resource - peakhour"
subcategory: ""
description: |-
  Manages ACME settings for a domain (e.g. the list of hostnames to include on an ACME certificate).
---

# peakhour_acme_settings (Resource)

Manages ACME settings for a domain (e.g. the list of hostnames to include on an ACME certificate).

## Example Usage

```terraform
resource "peakhour_acme_settings" "example" {
  domain       = "example.com"
  domain_names = ["example.com", "www.example.com"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) Domain name.

### Optional

- `domain_names` (List of String) Domain names to request via ACME. Set to null to clear.

### Read-Only

- `id` (String) ACME settings identifier (domain/acme_settings).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import peakhour_acme_settings.example example.com
```
